import "./NotFound.css"

const NotFound = () => {

    return (
        <div className="NotFound">
            <h2 className="NotFound-text">NotFound</h2>
        </div>
    );
};

export default NotFound;